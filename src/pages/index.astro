---
---
// HTML + JS dans une page Astro
<html lang="fr">
  <head>
    <meta charset="UTF-8" />
    <title>Zone secrète</title>
    <style>
      body {
        font-family: sans-serif;
        text-align: center;
        margin-top: 100px;
        background-color: #f0f0f0;
      }
      #secret {
        display: none;
        font-weight: bold;
        margin-top: 20px;
        background: white;
        padding: 1em;
        border-radius: 10px;
      }
    </style>
  </head>
  <body>
    <h2>Entrez le code pour accéder à l'info :</h2>
    <input type="password" id="code" placeholder="Code" />
    <button onclick="checkCode()">Valider</button>

    <div id="secret">🎉 Voici l'information secrète !</div>

    <script>
      function checkCode() {
        const userCode = document.getElementById("code").value;
        if (userCode === "jacques123") {
          document.getElementById("secret").style.display = "block";
        } else {
          alert("Code incorrect !");
        }
      }
    </script>
  </body>
</html>
